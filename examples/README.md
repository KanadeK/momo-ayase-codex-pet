# Examples

`policy.json` contains every supported PetEase threshold. Motion values create
warnings; structural values remain zero-tolerance.

```bash
petease audit ../pet --policy policy.json --strict
```

The release workflow publishes the real Momo audit in JSON, HTML, and SARIF.
Tests generate synthetic atlases at runtime so the software suite remains
independent from fan-art rights.
